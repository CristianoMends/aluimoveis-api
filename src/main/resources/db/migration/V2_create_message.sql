CREATE TABLE message (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   sender_id BIGINT,
   recipient_id BIGINT,
   content VARCHAR(255),
   timestamp TIMESTAMP WITHOUT TIME ZONE,
   CONSTRAINT pk_message PRIMARY KEY (id)
);

ALTER TABLE message ADD CONSTRAINT FK_MESSAGE_ON_RECIPIENT FOREIGN KEY (recipient_id) REFERENCES tab_user (user_id);

ALTER TABLE message ADD CONSTRAINT FK_MESSAGE_ON_SENDER FOREIGN KEY (sender_id) REFERENCES tab_user (user_id);